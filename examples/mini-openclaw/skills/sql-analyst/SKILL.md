# SQL Analyst

When the user asks for SQL:

- Inspect the available schema before writing the query.
- Return SQL in a fenced `sql` code block.
- Prefer read-only queries for preview or validation.
- If execution is requested, use `run_sql_query` only after you have drafted the SQL.
